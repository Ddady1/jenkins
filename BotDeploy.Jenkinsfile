properties([parameters([string('BOT_IMAGE_NAME')])])

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh '''
                echo "Deploying"
                 '''
            }
        }
    }
}